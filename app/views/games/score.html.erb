<h1>Your result</h1>

<% if @included %>
  <% if @english_word %>
    <p><%= @word %> is a valid word!</p>
  <% else %>
    <p>Sorry but <%= @word %> is not a valid word.. %></p>
  <% end %>
<% else %>
<p>Sorry but <%= @word %> can't be made out of <%= @letters.join(", ")%></p>
<% end %>

<%= link_to "Play again", new_path %>
